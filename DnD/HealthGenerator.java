public class HealthGenerator {
   private int hitDice = 0;
   public HealthGenerator(int numDice, int dieNum) {
      DiceRoller dice = new DiceRoller();
      for(hitDice; hitDice < numDice; hitDice++) {
         
      }
   }
}